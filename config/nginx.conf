server {
    listen       80;
    server_name  localhost;

    location ~* /api/(users) {
        proxy_pass http://localhost:6985;
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }
}